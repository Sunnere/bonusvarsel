name: Bonusvarsel – matrix test

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        program: [sas, turkish, lufthansa]

    steps:
      - name: Show matrix values
        run: |
          echo "Program: ${{ matrix.program }}"
